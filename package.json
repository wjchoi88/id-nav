{
  "name": "id-nav-chatbot",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "test": "npm run test -ws --if-present",
    "test:e2e": "node --test tests/e2e/pilot-flow.test.js"
  }
}
